name: double_multiply
bridge: mantik/tf.saved_model
protocol: 2
type:
  input:
    columns:
      x: float64
  output:
    columns:
      "y": float64 # Note: pure 'y' would be interpreted as "true" in YAML.