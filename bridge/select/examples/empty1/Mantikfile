# Let everything through, SELECT *
# Can still rename out columns
bridge: builtin/select
type:
    input:
        columns:
            "x": int32
            "y": string
    output:
        columns:
            a: int32
            b: string
selectProgram:
    selector: null
    projector: null
