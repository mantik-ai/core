package ai.mantik.planner.select.run

import ai.mantik.testutils.TestBase
import io.circe.Json
import io.circe.syntax._

class OpCodeSpec extends TestBase {

  it should "serialize them nice" in {
    pending
  }
}
